apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw05-gateway-user-chart.fullname" . }}
  labels:
    {{- include "hw05-gateway-user-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "hw05-gateway-user-chart.selectorLabels" . | nindent 4 }}
