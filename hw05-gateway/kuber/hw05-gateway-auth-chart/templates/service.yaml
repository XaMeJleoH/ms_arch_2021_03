apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw05-gateway-auth-chart.fullname" . }}
  labels:
    {{- include "hw05-gateway-auth-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "hw05-gateway-auth-chart.selectorLabels" . | nindent 4 }}
