apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw03-prometheus-chart.fullname" . }}
  labels:
    {{- include "hw03-prometheus-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "hw03-prometheus-chart.selectorLabels" . | nindent 4 }}
