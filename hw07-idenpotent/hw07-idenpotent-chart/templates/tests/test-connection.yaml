apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hw07-idenpotent.fullname" . }}-test-connection"
  labels:
    {{- include "hw07-idenpotent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hw07-idenpotent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
