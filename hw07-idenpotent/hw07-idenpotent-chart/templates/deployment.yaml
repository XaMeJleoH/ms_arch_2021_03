apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "hw07-idenpotent.fullname" . }}
  labels:
    {{- include "hw07-idenpotent.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "hw07-idenpotent.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "hw07-idenpotent.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "xamejleoh/{{ .Values.image.repository }}:{{ .Chart.AppVersion }}"
          ports:
            - name: web
              containerPort: 80
          envFrom:
            - configMapRef:
                name: ms-arch-2021-03-config
            - secretRef:
                name: ms-arch-2021-03-secret
