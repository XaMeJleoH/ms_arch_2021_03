apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw07-idenpotent.fullname" . }}
  labels:
    {{- include "hw07-idenpotent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "hw07-idenpotent.selectorLabels" . | nindent 4 }}
