apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hw09-saga-payment.fullname" . }}-test-connection"
  labels:
    {{- include "hw09-saga-payment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hw09-saga-payment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
