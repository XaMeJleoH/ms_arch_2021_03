apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw09-saga-payment.fullname" . }}
  labels:
    {{- include "hw09-saga-payment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "hw09-saga-payment.selectorLabels" . | nindent 4 }}
