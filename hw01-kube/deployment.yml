apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-arch-2021-03-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: hw01-kube-app
  template:
    metadata:
      labels:
        app: hw01-kube-app
    spec:
      containers:
        - name: hw01-kube
          image: hw01-kube:v1
          imagePullPolicy: Always
          ports:
            - name: web
              containerPort: 8000


